{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.userService.name }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.ingress.certIssuer }}
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - {{ .Values.userService.domain }}
    secretName: {{ .Values.userService.name }}-tls
  rules:
  - host: {{ .Values.userService.domain }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.userService.name }}
            port:
              number: 8004
{{- end }}
