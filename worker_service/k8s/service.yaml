apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: item-system
  labels:
    app: worker-service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: worker-service
