apiVersion: v1
kind: Service
metadata:
  name: item-service
  namespace: item-system
  labels:
    app: item-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: item-service
