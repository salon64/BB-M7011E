apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.itemService.name }}
spec:
  selector:
    app: {{ .Values.itemService.name }}
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
